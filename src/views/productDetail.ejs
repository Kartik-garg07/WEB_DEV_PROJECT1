<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.title %> - Details</title>
    <link rel="stylesheet" href="/productDetail.css">
</head>
<body>
    <div class="card">
        <div class="left">
            <img src="<%= product.image %>" alt="">
        </div>
        <div class="right">
            <h2><%= product.title %></h2>
            <p><strong>Description:</strong> <%= product.description %></p>
            <p><strong>Category:</strong> <%= product.category %></p>
            <h3><strong>Price:</strong> <%= product.price %></h3>
            <div class="buttons">
                <% if (email === "Kartik@gmail.com") { %>
                    <button><a href="/products/update/<%= product._id %>">Update</a></button>
                    <button><a href="/products/delete/<%= product._id %>">Delete</a></button>
                <% } else { %>
                    <button><a href="/products/buy/<%= product._id %>">Buy Now</a></button>
                    <% if (addedToCart) { %>
                        <span style="color: green; font-weight: bold;">Added to Cart</span>
                    <% } else { %>
                        <button><a href="/products/add-to-cart/<%= product._id %>">Add to Cart</a></button>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>